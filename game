rs(body:GetChildren()) do
            if child:IsA("VehicleSeat") then
                seat = child
                break
            end
        end
        if seat and seat.Name == "Marked" then return true end
    end

    return false
end

local function isCarAlive()
    local m = getMyVehicleModel()
    if not m or not m.PrimaryPart then return false end
    if isCarBroken(m) then return false end
    if VehicleManager and VehicleManager.IsDriving then
        local ok, driving = pcall(function() return VehicleManager:IsDriving() end)
        if ok and not driving then return false end
    end
    return true
end

local function getDesiredCar()
    local m = State.ManualCar
    local goodOverride =
        type(m) == "string"
        and m ~= ""
        and m:sub(1, 1) ~= "("
        and table.find(VehicleOrder, m) ~= nil
    if goodOverride then return m end

    local owned = refreshOwned()
    local best, bestIdx = nil, 0
    for _, name in ipairs(owned) do
        if type(name) == "string"
           and name ~= ""
           and name:sub(1, 1) ~= "(" then
            local idx = table.find(VehicleOrder, name)
            if idx and idx > bestIdx then
                best, bestIdx = name, idx
            end
        end
    end
    return best
end

local function spawnCooldownRemaining()
    if not VehicleSpawning or not TimeKeeper then return 0 end
    local ok, endTime = pcall(VehicleSpawning.GetSpawnCooldownEndTime, "Dealership")
    if not ok or not endTime then return 0 end
    local now = TimeKeeper.GetTime()
    local remaining = endTime - now
    return remaining > 0 and remaining or 0
end
local function isValidCarNameInline(n)
    return type(n) == "string"
        and n ~= ""
        and n:sub(1, 1) ~= "("
        and table.find(VehicleOrder, n) ~= nil
end

local function fireSpawnRemoteSafe(name)
    if not isValidCarNameInline(name) then
        warn("[CC2-AutoFarm] BLOCKED spawn remote with bad name: " .. tostring(name))
        return false
    end
    SpawnRemote:FireServer("Dealership", name, true)
    return true
end

local function spawnCar(name)
    if not isValidCarNameInline(name) then
        warn("[CC2-AutoFarm] spawnCar refused: not a real car name: " .. tostring(name))
        local fallback
        for _, n in ipairs(refreshOwned()) do
            if isValidCarNameInline(n) then
                local idx = table.find(VehicleOrder, n)
                if idx and (not fallback or idx > (table.find(VehicleOrder, fallback) or 0)) then
                    fallback = n
                end
            end
        end
        if not fallback then return false end
        warn("[CC2-AutoFarm] auto-fallback to best owned: " .. fallback)
        name = fallback
    end
    local cd = spawnCooldownRemaining()
    if cd > 0 then
        status(("Spawn cooldown %.1fs"):format(cd))
        task.wait(cd + 0.1)
    end

    if not isValidCarNameInline(name) then
        warn("[CC2-AutoFarm] BLOCKED spawnCar after cooldown: " .. tostring(name))
        return false
    end

    if VehicleSpawning and VehicleSpawning.SpawnYield then
        local ok, res = pcall(function()
            return VehicleSpawning.SpawnYield("Dealership", name, true)
        end)
        if ok then return res ~= nil end
    end
    return fireSpawnRemoteSafe(name)
end

local function crushPulse()
    local model = getMyVehicleModel()
    if not model or not model.PrimaryPart then return false end

    local ok = pcall(function()
        model:PivotTo(model:GetPivot() + Vector3.new(0, State.PulseLift, 0))
    end)
    if not ok then return false end

    RunService.Heartbeat:Wait()

    pcall(function()
        local v = Vector3.new(0, -State.PulseSlam, 0)
        for _, p in ipairs(model:GetDescendants()) do
            if p:IsA("BasePart") and not p.Anchored then
                p.AssemblyLinearVelocity = v
            end
        end
    end)
    return true
end

local pulseCount = 0

task.spawn(function()
    while true do
        task.wait(State.PulseInterval)

        if State.AutoBuyPlatina then pcall(autoBuyPlatina) end

        if not State.AutoFarm then continue end

        local model = getMyVehicleModel()

        if model and model.PrimaryPart and not isCarBroken(model) then
            crushPulse()
            pulseCount = pulseCount + 1
            if pulseCount % 5 == 0 then
                status(("Pulsing… (%d)"):format(pulseCount))
            end
        else
            if model and isCarBroken(model) then
                status(("Car broken after %d pulses"):format(pulseCount))
            end
            pulseCount = 0

            local car = getDesiredCar()
            if not car then
                status("No owned car to spawn")
                task.wait(2)
            else
                if State.AutoPaint then pcall(applyPlatinumPaint, car, false) end

                status("Spawning " .. car)
                spawnCar(car)
                local t0 = os.clock()
                while os.clock() - t0 < 8 do
                    local m = getMyVehicleModel()
                    if m and m.PrimaryPart and not isCarBroken(m) then break end
                    task.wait(0.2)
                end
            end
        end
    end
end)

if VehicleSpawning and VehicleSpawning.SpawnCooldownChanged
   and VehicleSpawning.SpawnCooldownChanged.Connect then
    VehicleSpawning.SpawnCooldownChanged.Connect(function()
    end)
end

local window = GuiLibrary:CreateWindow("Despair verso — Discord: emptyverso", UDim2.new(0, 560, 0, 440))
local Main   = GuiLibrary:CreateTab(window, "Farm")
local Buying = GuiLibrary:CreateTab(window, "Other")
local Misc   = GuiLibrary:CreateTab(window, "Misc")

GuiLibrary:CreateSection(Main, "Auto Farm")

GuiLibrary:CreateToggle(Main, "Auto Farm", false, function(v) State.AutoFarm = v end)

GuiLibrary:CreateSlider(Main, "Pulse Lift (studs)",  20, 150, 80, function(v) State.PulseLift = v end)
GuiLibrary:CreateSlider(Main, "Pulse Slam (studs/s)", 100, 600, 350, function(v) State.PulseSlam = v end)
GuiLibrary:CreateSlider(Main, "Pulse Interval (x100ms)", 2, 15, 4, function(v)
    State.PulseInterval = v / 10
end)

GuiLibrary:CreateSection(Main, "Manual")

GuiLibrary:CreateButton(Main, "Spawn Best Owned", function()
    local c = getDesiredCar()
    if not c then
        GuiLibrary:CreateNotification("Spawn", "No owned car found", 2, "error"); return
    end
    spawnCar(c)
    GuiLibrary:CreateNotification("Spawn", "Spawning " .. c, 2, "info")
end)

GuiLibrary:CreateButton(Main, "Crush Pulse Now", function()
    if not isCarAlive() then
        GuiLibrary:CreateNotification("Crush", "No live car", 2, "error"); return
    end
    crushPulse()
end)

GuiLibrary:CreateSection(Buying, "Platina")

GuiLibrary:CreateToggle(Buying, "Auto Buy Platina Cars (cheapest first)", false, function(v)
    State.AutoBuyPlatina = v
end)

GuiLibrary:CreateButton(Buying, "Buy All Affordable Platina Cars", function()
    autoBuyPlatina()
    GuiLibrary:CreateNotification("Buying", "Platina sweep done", 2, "success")
end)

GuiLibrary:CreateButton(Buying, "Refresh Owned List", function()
    local list = refreshOwned(true)
    GuiLibrary:CreateNotification("Owned", ("%d cars owned"):format(#list), 2, "info")
end)


GuiLibrary:CreateSection(Buying, "Platinum Paint 3x money (Gamepass only)")

GuiLibrary:CreateToggle(Buying, "Auto Apply Platinum Paint", false, function(v)
    State.AutoPaint = v
end)

GuiLibrary:CreateButton(Buying, "Apply Platinum Current Car", function()
    local car = getDesiredCar()
    if not car then
        GuiLibrary:CreateNotification("Paint", "No car selected", 2, "error"); return
    end
    applyPlatinumPaint(car, true)
    GuiLibrary:CreateNotification("Paint", "Painted " .. car, 2, "success")
end)

GuiLibrary:CreateButton(Buying, "Apply Platinum All Owned Cars", function()
    local list = refreshOwned(true)
    local n = 0
    for _, name in ipairs(list) do
        if applyPlatinumPaint(name, true) then n = n + 1 end
        task.wait(0.05)
    end
    GuiLibrary:CreateNotification("Paint", ("Painted %d cars"):format(n), 3, "success")
end)

GuiLibrary:CreateButton(Buying, "Reset Paint Cache (re-paint everything)", function()
    paintedCars = {}
    GuiLibrary:CreateNotification("Paint", "Cache cleared", 2, "info")
end)

GuiLibrary:CreateSection(Misc, "Manual Override")

local carOptions = { "(auto: best owned)" }
for _, n in ipairs(VehicleOrder) do table.insert(carOptions, n) end

GuiLibrary:CreateDropdown(Misc, "Spawn Override", carOptions, function(choice)

    if type(choice) ~= "string"
       or choice == ""
       or choice:sub(1, 1) == "("
       or not table.find(VehicleOrder, choice) then
        State.ManualCar = nil
    else
        State.ManualCar = choice
    end
end)

GuiLibrary:CreateLabel(Misc, "Override forces a specific car. '(auto: best owned)' = use best.")

GuiLibrary:CreateSection(Misc, "Diagnostics")

GuiLibrary:CreateButton(Misc, "Show Status", function()
    GuiLibrary:CreateNotification("Status", State.StatusText, 3, "info")
end)

GuiLibrary:CreateButton(Misc, "Show Best Owned", function()
    GuiLibrary:CreateNotification("Best Owned", getBestOwned() or "(none)", 3, "info")
end)

GuiLibrary:CreateButton(Misc, "Show Wallet", function()
    GuiLibrary:CreateNotification("Wallet",
        ("Money %s | Platina %s"):format(tostring(getMoney()), tostring(getPlatina())),
        3, "info")
end)

GuiLibrary:CreateButton(Misc, "Show Car State", function()
    local m       = getMyVehicleModel()
    local broken  = m and isCarBroken(m) or false
    local alive   = isCarAlive()
    local cd      = spawnCooldownRemaining()
    GuiLibrary:CreateNotification("Car",
        ("Alive: %s | Broken: %s | Model: %s | CD %.1fs"):format(
            tostring(alive), tostring(broken), m and m.Name or "nil", cd),
        3, "info")
end)

GuiLibrary:CreateNotification("Auto Farm v6", "Loaded. Made by Despair verso | Discord: emptyverso", 3, "success")
status("Loaded v6")